{
  "name": "universal-edge-template",
  "version": "1.0.0",
  "description": "",
  "main": "cloudflare-entry.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:cf": "wrangler dev cloudflare-entry.ts",
    "dev:deno": "deno run deno-entry.ts --allow-all",
    "deploy:cf": "wrangler publish",
    "deploy:deno": "deployctl deploy --project=PROJECT_NAME --token=API_TOKEN ./deno-entry.ts"
  },
  "keywords": [
    "universal-edge-template"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bottlejs": "^2.0.1",
    "hono": "^3.10.0"
  },
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "esbuild": "^0.15.1",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.1.2",
    "jest-environment-miniflare": "^2.6.0",
    "types-cloudflare-worker": "^1.2.0",
    "prettier": "^3.1.0",
    "wrangler": "^2.15.0"
  }
}
